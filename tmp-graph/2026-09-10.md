# 2026-09-10

## School Portal — authorization, alerts, timetable, study plans
- Portal repository: `/home/admin/.openclaw/workspace/sjabrankamran-site/`; production domain: `https://sjabrankamran.com`.
- Commit `6ea4ad4` fixed a critical authorization leak: coordinator, facilitator, and attendance-registrar accounts are now constrained server-side to their assigned school **and explicitly assigned active classes**. Scoped staff cannot use global admin/mail/progress/analytics APIs, select other schools/classes, or broadcast beyond assigned classes. Enrolled staff are excluded from student recipient and attendance lists. LACAS facilitator had two active assignments (G1 and G2) at the time of verification.
- Commit `ba5cfd1` added notification/PWA alert infrastructure and private Google Calendar subscription feeds for tests, assignments, tasks/challenges, scheduled classes, and extra classes. Web Push code exists but OS push still requires valid VAPID environment variables in Vercel.
- Commit `6daa7cf` expanded “Preview interface as” to all previewable portal roles: student, parent, teacher, teaching assistant, facilitator, coordinator, counsellor, attendance registrar, content manager, finance manager, and admin. Super admin is intentionally excluded.
- Commit `0a2b520` deployed the all-schools timetable and broader student-support system:
  - 31/31 recurring timetable rows verified against the uploaded DOCX; no missing or unexpected rows.
  - New role-filtered `/portal/timetable`; super admin sees all schools while students, parents, and scoped staff see only assigned cohorts.
  - Regular/extra classes flow into notifications, PWA/in-app alerts, and Google Calendar feeds.
  - New `/portal/study-plan` automatically targets weaknesses/ranking with sequenced study materials, video/simulation links, written assignments, short tests, and mandatory daily challenges, while recording allocations/submissions/completion.
  - Mandatory student onboarding gate now requires a student photo, student WhatsApp, and at least one guardian’s name, valid email, and WhatsApp; incomplete students cannot access other portal services on next active session.
  - Google Drive archive root `sjabrankamran` created with Students, Tests, Assignments, Physics Resource, and Resource Library branches; student photos, answer scripts, assignments, community uploads, and admin resources are mirrored.
  - Saturday parent progress reports scheduled for 18:00 Asia/Karachi, personalised to guardian names and covering attendance, performance, weaknesses, assignments, and study-plan completion.
- Remaining activation gaps:
  - Parent email delivery is not fully proven because the connected Google authorization lacks Gmail send permission and no active alternative mail relay was available.
  - PWA operating-system push needs a valid VAPID private key and related Vercel environment configuration; in-app notifications and calendar subscriptions work independently.
- Security incident note: credentials/tokens were pasted into chat during setup. They must be treated as exposed and revoked/rotated; never reuse or store the transcript values. Use only host-owned masked credential entry.
- Git pushes from this OpenClaw host may require Git to use the current rotating secret-egress proxy trust bundle; keep TLS verification enabled and never work around it with `sslVerify=false`.

## Class Test 1 · Physical Quantities & Units (scheduled 11 Sep 2026)
- Authored 20 CAIE-style 9702 MCQs (7 LOT / 8 MOT / 5 HOT; base units, constant units, unitless proofs, unknown powers, homogeneity) as rendered card PNGs in `exam-assets/custom/9702_ct1_pqu/`. Question data + answer key + renderer live locally in gitignored `tmp-ct1/` of the site repo.
- Commit `6f6e01d`: `src/lib/exam-lab/secure-bank.json` (allocation-only bank, excluded from public practice/drills) + `FULL_BANK` resolution + per-student question shuffle for test-mode custom allocations. Marking is keyed by question id so autocheck is arrangement-proof.
- Allocations (mode `test`, proctored, 30 min, ids `ct1-lacas-sep11-7pm` / `ct1-lgs55-sep11-9pm`): Lacas JT A1 G1+G2 (91 students, opens 19:00 PKT) and LGS 55 Main A1 G1+G2 (74 students, opens 21:00 PKT); due = open + 1 h. 165 portal announcements (kind `test`) inserted. Verified: signed card URL 200, sample allocation doc correct, announcement count 165. Students launch from /portal/exam-lab; startsAt gating prevents early starts. <!-- project: path:/home/admin/.openclaw/workspace/main -->
- Commit `4f62299` added direct Gmail REST delivery to the central portal mail transport while retaining the existing Apps Script relay fallback. User-creation/reset credential emails, manual mail, queued mail flushes, and Saturday parent reports all use this one transport. The authoritative Google credential is loaded from protected Supabase Storage and successfully accesses Drive as `physics@sjabrankamran.com`, but a read-only Gmail profile probe on 2026-09-10 returned HTTP 403 `PERMISSION_DENIED`; therefore the current refresh token still lacks Gmail scope even though consent may have been approved. Messages queue safely until a Gmail-scoped refresh token or Apps Script relay is active. Typecheck/build passed and Vercel deployed `4f62299` successfully.

## Portal task-opening repair
- Commit `e6401d0` fixed personal assignments/daily challenges that appeared not to open. Root causes: the prominent Start control only changed status, some generated topic labels did not exist in the Exam Lab image bank, and ranking level was incorrectly used as AS/A2 course placement when choosing P2/P4.
- Added an owned `/portal/tasks/[id]` detail/submission page, exact `/portal/exam-lab?allocation=<id>` deep-links, automatic launch of the referenced allocation, automatic linked-task completion on Exam Lab submission, and a non-silent question-pool fallback.
- Repaired live Storage records: 18 invalid-topic allocation docs initially, then 3 wrong-paper study-plan docs and 38 task docs. Final audit: 70 automated allocations, 0 empty question pools; 75 linked tasks, 0 broken deep-links. TypeScript and production build passed; Vercel deployed `e6401d0` successfully.

## Private portal QA student
- Created a private demo student (UID `861414dd-f76d-4c03-b0d1-13be6ca6303e`) enrolled in LACAS JT AS Year 1 G1 to inspect the real student-facing portal. Account is active with student role, completed placeholder onboarding/profile photo/guardian data, three Exam Lab allocations (two scheduled tests plus one daily challenge), two personal tasks, and three notifications. Password login was verified.
- The original root-only credential link was inaccessible from WebChat. Commit `abfec84` added `/portal/admin/demo-student-access`, restricted server-side to the real `super_admin` role. It generates/returns a fresh password only on an authenticated POST, sets `Cache-Control: no-store`, and audits the rotation; no password is committed or publicly linked. Vercel production deployment `6377832113` succeeded. A fallback credential sheet remains only in `/root/private-credentials-archive/` (root:root, mode 600; archive mode 700); no credential-like files exist in `/srv/pablo-dl`.

## Student weekly study-plan visibility repair
- Commit `3bf45a6` fixed the personalised weekly study-plan surface. Root causes: Super Admin's “Preview as Student” navigation exposed `/portal/study-plan`, but the page checked the real admin role and redirected; the page also ignored its server-generated tasks and could render a blank activity section after a failed client fetch.
- The student dashboard now has a prominent personalised-weekly-plan card with priority topics and mandatory outstanding count. `/portal/study-plan` renders server-generated activities immediately, has a visible loading/empty state, and uses the private QA student record in read-only Student preview so no tasks are written against the administrator. TypeScript and production build passed; Vercel deployed `3bf45a6` successfully.
